# ═══════════════════════════════════════════════════════════════════════
# Titanium v12 — Dépendances complètes
# ═══════════════════════════════════════════════════════════════════════

# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
jsonschema>=4.23.0,<5.0.0

# MCP / multi-agents (collab_hub/mcp.py, mcp_server.py, mcp_base44.py — serveurs stdio)
mcp>=1.27.0                    # requis par collab_hub ; son absence bloquait `pytest tests/` (test_collab_hub_api)

# Broker data (data/mt5_provider.py — DATA-ONLY, Windows uniquement ; correction Copilot 2026-07-25)
MetaTrader5>=5.0.45 ; platform_system == "Windows"

# Data & calcul
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
pandas-ta>=0.3.14b

# Assistant / JARVIS
scikit-learn>=1.4.0            # intent_classifier.py — SVM classification
faster-whisper>=1.0.0          # voice_engine.py — STT
piper-tts>=1.2.0               # tts_engine.py — synthèse vocale locale
websockets>=12.0               # signal_alert.py — bridge WS JARVIS

# Browser agent (optionnel — pip install playwright && playwright install)
ddgs>=6.0                      # browser_agent.py — DuckDuckGo search
beautifulsoup4>=4.12.0         # browser_agent.py — parsing HTML
lxml>=5.0.0                    # browser_agent.py — parser rapide
# playwright>=1.40.0           # Optionnel : navigation headless

# Monitoring & hotkeys
pynput>=1.7.6                  # hotkey_manager.py — Ctrl+Alt
structlog>=24.0.0
