================================================================================
  TITANIUM v12 + TITAN ASSISTANT — TOUTES LES COMMANDES D'EXECUTION
  Mise a jour : avril 2026
================================================================================

REPERTOIRE DE TRAVAIL : C:\Users\flore\Desktop\TITANIUM\Titanium\v12
================================================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. LANCEMENT PRINCIPAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  cd C:\Users\flore\Desktop\TITANIUM\Titanium\v12

  # Lancer le bot complet (Titanium + Titan assistant si TITAN_ENABLED=1)
  python main.py

  # Dashboard disponible sur :
  http://localhost:8080

  # Avatar Titan disponible sur :
  http://localhost:8080/assistant/


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  2. OLLAMA — LLM LOCAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Demarrer Ollama (si pas deja en service)
  ollama serve

  # Verifier les modeles installes
  ollama list

  # Telecharger phi3:mini (recommande — 2.2 GB)
  ollama pull phi3:mini

  # Alternatives plus legeres
  ollama pull qwen2:1.5b       # 950 MB — plus rapide
  ollama pull tinyllama         # 640 MB — ultra leger

  # Tester le modele
  ollama run phi3:mini

  # Verifier que Ollama repond
  curl http://localhost:11434/api/tags

  # Arreter Ollama
  taskkill /IM ollama.exe /F   (Windows)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  3. GITNEXUS — ANALYSE DE CODE IA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  cd C:\Users\flore\Desktop\TITANIUM\Titanium\v12

  # Indexer le projet (a faire apres chaque commit)
  npx gitnexus analyze

  # Indexer avec embeddings IA (plus precis, plus lent)
  npx gitnexus analyze --embeddings

  # Demarrer le serveur GitNexus (MCP)cd desk
  npx gitnexus server

  # Verifier le statut de l'index
  npx gitnexus status

  # Nettoyer l'index
  npx gitnexus clean

  # Installer Node.js si npx absent :
  https://nodejs.org/en/download


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  4. TITAN — ASSISTANT VOCAL IA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Installer les dependances Titan (une seule fois)
  python -m pip install faster-whisper sounddevice webrtcvad-wheels pywebview pynput keyboard numpy

  # Telecharger voix francaise Piper
  curl -L "https://huggingface.co/rhasspy/piper-voices/resolve/v1.0.0/fr/fr_FR/upmc/medium/fr_FR-upmc-medium.onnx" -o assistant\voices\fr_FR-upmc-medium.onnx
  curl -L "https://huggingface.co/rhasspy/piper-voices/resolve/v1.0.0/fr/fr_FR/upmc/medium/fr_FR-upmc-medium.onnx.json" -o assistant\voices\fr_FR-upmc-medium.onnx.json

  # Telecharger Piper TTS (executable Windows)
  curl -L https://github.com/rhasspy/piper/releases/download/2023.11.14-2/piper_windows_amd64.zip -o assistant\piper\piper.zip
  cd assistant\piper && tar -xf piper.zip && del piper.zip && cd ..\..

  # Script d'installation automatique
  python assistant\install_titan.py

  # Activer Titan dans .env
  # Mettre TITAN_ENABLED=1 dans le fichier .env

  # Raccourci vocal : Ctrl+Alt (maintenir -> parler -> relacher)

  # Interface avatar :
  http://localhost:8080/assistant/


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  5. API TITANIUM — ENDPOINTS PRINCIPAUX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Etat global du bot
  curl http://localhost:8080/api/state

  # Signaux actifs
  curl http://localhost:8080/api/state | python -m json.tool

  # Paper trading — etat
  curl http://localhost:8080/paper/state

  # Paper trading — statistiques
  curl http://localhost:8080/paper/stats

  # Paper trading — positions ouvertes
  curl http://localhost:8080/paper/positions

  # Paper trading — historique des trades
  curl http://localhost:8080/paper/trades

  # Paper trading — courbe equity
  curl http://localhost:8080/paper/equity-curve

  # Paper trading — remettre a zero
  curl -X POST http://localhost:8080/paper/reset

  # Paper trading — reset circuit breaker
  curl -X POST http://localhost:8080/paper/reset-circuit-breaker

  # Optimisation — resultats
  curl http://localhost:8080/api/optim/results

  # Optimisation — relancer manuellement
  curl -X POST http://localhost:8080/api/optim/run

  # Delta volume
  curl http://localhost:8080/api/delta-vol

  # Metrics
  curl http://localhost:8080/api/metrics

  # Fundamentals — etat macro
  curl http://localhost:8080/fundamentals/state

  # Webhook TradingView — statut
  curl http://localhost:8080/webhook/status

  # Webhook TradingView — positions pour TV
  curl http://localhost:8080/webhook/positions-for-tv

  # Services — etat tous les services
  curl http://localhost:8080/services/status


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  6. API TITAN — COMMANDES VOCALES ET AVATAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Faire parler Titan (texte direct -> voix + avatar)
  curl -X POST http://localhost:8080/titan/speak -H "Content-Type: application/json" -d "{\"text\": \"Bonjour, Titan en ligne.\"}"

  # Envoyer une commande a Titan (LLM -> reponse vocale)
  curl -X POST http://localhost:8080/titan/command -H "Content-Type: application/json" -d "{\"command\": \"quel est le signal BTC ?\"}"

  # Commandes utiles pour Titan :
  #   "quel est le signal BTC ?"
  #   "fais le rapport du jour"
  #   "quel est mon winrate ?"
  #   "quel est le risque macro ?"
  #   "analyse les positions ouvertes"
  #   "combien j'ai gagne aujourd'hui ?"

  # Statut de Titan
  curl http://localhost:8080/titan/status

  # Effacer l'historique de conversation
  curl -X POST http://localhost:8080/titan/clear-history

  # Controle des services via dashboard
  curl -X POST http://localhost:8080/services/ollama/start
  curl -X POST http://localhost:8080/services/ollama/stop
  curl -X POST http://localhost:8080/services/gitnexus/start
  curl -X POST http://localhost:8080/services/gitnexus/stop
  curl -X POST http://localhost:8080/services/titan/start
  curl -X POST http://localhost:8080/services/titan/stop

  # Alexa webhook (futur)
  curl http://localhost:8080/alexa/status


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  7. INSTALLATION COMPLETE — ORDRE RECOMMANDE (premiere fois)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # 1. Dependances Python principales
  cd C:\Users\flore\Desktop\TITANIUM\Titanium\v12
  python -m pip install fastapi uvicorn aiohttp numpy pandas pandas_ta python-dotenv psutil websockets

  # 2. Dependances Titan
  python -m pip install faster-whisper sounddevice webrtcvad-wheels pywebview pynput keyboard

  # 3. Piper TTS (voix)
  curl -L https://github.com/rhasspy/piper/releases/download/2023.11.14-2/piper_windows_amd64.zip -o assistant\piper\piper.zip
  cd assistant\piper && tar -xf piper.zip && del piper.zip && cd ..\..
  curl -L "https://huggingface.co/rhasspy/piper-voices/resolve/v1.0.0/fr/fr_FR/upmc/medium/fr_FR-upmc-medium.onnx" -o assistant\voices\fr_FR-upmc-medium.onnx
  curl -L "https://huggingface.co/rhasspy/piper-voices/resolve/v1.0.0/fr/fr_FR/upmc/medium/fr_FR-upmc-medium.onnx.json" -o assistant\voices\fr_FR-upmc-medium.onnx.json

  # 4. Ollama + modele LLM
  # Telecharger Ollama : https://ollama.com/download
  ollama pull phi3:mini

  # 5. GitNexus
  # Installer Node.js : https://nodejs.org
  npx gitnexus analyze

  # 6. Activer Titan dans .env
  # Mettre TITAN_ENABLED=1

  # 7. Lancer
  python main.py


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  8. TESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Lancer tous les tests
  cd C:\Users\flore\Desktop\TITANIUM\Titanium\v12
  python -m pytest tests/ -v

  # Tests paper trading uniquement
  python -m pytest tests/test_paper_trading.py -v

  # Tests fundamentals
  python -m pytest tests/test_risk_scorer.py tests/test_modulator.py -v

  # Test import complet (verification de sante)
  python -c "from api.api_server import app; print('OK —', len(app.routes), 'routes')"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  9. FICHIERS DE CONFIGURATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  .env                          Toutes les variables de configuration
  data/paper_journal.json       Journal des trades paper (JSON)
  data/paper_journal.csv        Journal des trades paper (CSV)
  data/paper_state.json         Etat persistant du compte paper
  data/signal_history.json      Historique des signaux (learning engine)
  data/scoring_weights.json     Poids adaptatifs par symbole

  # Editer la configuration
  notepad C:\Users\flore\Desktop\TITANIUM\Titanium\.env


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  10. DEPANNAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # BTC ne retourne aucune valeur
  # -> Normal au demarrage, attendre 5 minutes que les barres 30s s'accumulent
  # -> Voir les logs : "[SCAN] BTC/USDT — df30 insuffisant (X barres, min=10)"

  # Circuit breaker actif (signaux bloques)
  curl -X POST http://localhost:8080/paper/reset-circuit-breaker

  # Titan ne parle pas
  # 1. Verifier TITAN_ENABLED=1 dans .env
  # 2. Verifier piper.exe : assistant\piper\piper.exe
  # 3. Verifier la voix : assistant\voices\fr_FR-upmc-medium.onnx
  # 4. Tester : curl -X POST http://localhost:8080/titan/speak -H "Content-Type: application/json" -d "{\"text\": \"test\"}"

  # Ctrl+Alt ne fonctionne pas
  # -> Installer pynput : python -m pip install pynput
  # -> Redemarrer main.py

  # Ollama trop lent
  # -> Passer a un modele plus leger : TITAN_LLM_MODEL=qwen2:1.5b dans .env
  # -> Reduire TITAN_LLM_MAX_TOKENS=150

  # Avatar VRM ne charge pas
  # -> Ouvrir F12 dans le navigateur pour voir l'erreur
  # -> Hard reload : Ctrl+Shift+R

================================================================================
