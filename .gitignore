# ── Secrets — NE JAMAIS COMMITTER ────────────────────────────────────────────
.env
.env.local
.env.*.local
*.key
secrets.json

# ── Données runtime (générées automatiquement) ────────────────────────────────
signal_history.json
scoring_weights.json
data/risk_history.json
titanium_v12.log
*.log

# ── Cache Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.mypy_cache/
.ruff_cache/

# ── Outils IDE / OS ───────────────────────────────────────────────────────────
!.vscode/
.vscode/*
!.vscode/tasks.json
.idea/
.windsurf/
.codeium/
*.swp
.DS_Store
Thumbs.db

# ── Claude Code (sessions, cache) ─────────────────────────────────────────────
.claude/

# ── GitNexus index ────────────────────────────────────────────────────────────
.gitnexus/

# ── Environnements & outils locaux (ne jamais committer) ──────────────────────
venv/
.venv/
gitnexus/
.pytest_cache/
antigravity_config/
data/events.jsonl
data/trade_journal.jsonl

# venv de test reproductible (Codex)
.venv_test/

# python embeddable local pour la verif Codex (sandbox-accessible)
.pyembed/

# Repertoires temporaires de tests locaux (certains gardent une ACL pytest fermee)
.pytest-tmp-*/
pytest-tmp-*/
codex_pytest_tmp_*/
.test-tmp/
.tmp_pytest_*/

# Sauvegardes de configuration et caches locaux (peuvent contenir des secrets)
.env.bak*
.test-tmp-*/
.obsidian/
.vscode/

# Artefacts de travail locaux, non livrables
2026-07-10.md
Sans titre.base
DESIGN-IS-*/
PATHFINDER-*/
REVIEWS/

# Worktrees Git locaux pour les chantiers agentiques isolés
.worktrees/

# ── Dépendances Node (jamais sur GitHub) ──────────────────────────────────────
node_modules/

# ── État interne des outils agentiques (jetons de session, pas le projet) ─────
.superpowers/
.hermes/
.agents/

# ── Sauvegardes de clés (publiques, mais une .bak n'a rien à faire dans le dépôt)
collab/governance/*keys*.bak*
*.bak-20*

# ── Données lourdes / runtime / régénérables — JAMAIS poussées (>1 Go local) ──
data/calibration_*/
data/opt_cache/
data/control_event_plane/
data/runtime/
data/codex_tmp/
data/collab_hub/
data/acl_backups/
data/mt5_reports/
data/*.png
data/demo_journal.ndjson
data/health_journal.jsonl
data/health_status.json
data/_relaunch_*.txt
docs/mt5_opt_reports/

# Journal SQLite runtime (patrimoine local, jamais en git ni en dossier synchronisé)
data/journal/

# Memoire persistante de Cloe (donnee locale, jamais en git)
data/cloe/memory.json
